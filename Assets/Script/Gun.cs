using UnityEngine;

public class Gun : MonoBehaviour
{
    private float rotateOffset = 180f;
    void Start()
    {
        
    }

    void Update()
    {
        
    }

    void RotateGun()
    {
        // Vector 3 displacement = transform.posision - Camera.main.ScreenToWorldPoint(Input.mousePosition);
        // float angle = Mathf 
    }
}
